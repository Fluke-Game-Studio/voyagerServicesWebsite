Options -MultiViews
RewriteEngine On

# React Router fallback for direct hits like /contact, /qa, etc.
RewriteCond %{REQUEST_FILENAME} -f [OR]
RewriteCond %{REQUEST_FILENAME} -d
RewriteRule ^ - [L]

RewriteRule ^ index.html [L]
